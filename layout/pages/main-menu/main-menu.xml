<root>
	<styles>
		<include src="file://{styles}/pages/main-menu/index.scss" />
	</styles>

	<scripts>
		<include src="file://{scripts}/pages/main-menu/index.js" />
	</scripts>

	<!-- The game will crash if these panels are not present. This will be fixed! -->
	<snippets>
		<snippet name="MainMenuMovieSnippet">
			<Panel>
				<Movie id="MainMenuMovie" hittest="false" ></Movie>
			</Panel>
		</snippet>
	</snippets>

	<ChaosMainMenu>

		<!-- The game will crash if this panel is not present. This will be fixed! -->
		<Panel id="MainMenuMovieParent" />

		<Panel id="MainMenuInput">

			<!-- The contents of your page goes here! -->
			<Panel class="center">
				<Label class="title" text="Hello world!" />
				<Label class="subtitle" text="This is a barebones Panorama template." />
				<TextButton onactivate="quit()" text="Quit" />
			</Panel>

		</Panel>

		<!-- Make sure these are at the bottom to get the z-order right -->
		<ChaosPopupManager id="PopupManager" />
		<ContextMenuManager id="ContextMenuManager" />
		<ChaosTooltipManager id="TooltipManager" hittest="false" />

		<!-- Doesn't actually include the notifications, but we use its position to figure out where to show them -->
		<Panel id="SteamNotificationsPlaceholder" hittest="false" />

	</ChaosMainMenu>
</root>